import { InfoMessage } from "../../../../reusable/InfoMessage/InfoMessage";

export const EmptyOrderHistory = () => {
  return (
    <>
      <InfoMessage subtitle={"You have no orders."} />
    </>
  );
};
